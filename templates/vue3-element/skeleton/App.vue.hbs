<template>
  <el-container>
    <el-aside width="220px">
      <el-menu
        :default-active="activeMenu"
        router
        background-color="#304156"
        text-color="#bfcbd9"
        active-text-color="#409EFF"
      >
        <el-menu-item index="/">
          <el-icon><HomeFilled /></el-icon>
          <span>首页</span>
        </el-menu-item>
        {{#each tables}}
        <el-menu-item index="/{{kebabCase name}}">
          <el-icon><Document /></el-icon>
          <span>{{className}}</span>
        </el-menu-item>
        {{/each}}
      </el-menu>
    </el-aside>
    <el-main>
      <router-view />
    </el-main>
  </el-container>
</template>

<script setup lang="ts">
import { computed } from 'vue';
import { useRoute } from 'vue-router';

const route = useRoute();
const activeMenu = computed(() => route.path);
</script>

<style>
body {
  margin: 0;
}
.el-container {
  min-height: 100vh;
}
.el-aside {
  background-color: #304156;
}
.el-main {
  background-color: #f0f2f5;
  padding: 20px;
}
</style>
