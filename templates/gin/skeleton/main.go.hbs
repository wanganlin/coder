package main

import (
	"{{package}}/db"
	"github.com/gin-gonic/gin"
)

func main() {
	// 初始化数据库
	db.InitDB()

	// 初始化 Gin 引擎
	r := gin.Default()

	// 注册路由
	RegisterRoutes(r)

	// 监听并启动服务
	r.Run(":8080")
}
