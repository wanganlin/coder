version: '3.8'

services:
  app:
    build: .
    ports:
      - '8080:8080'
    environment:
      - SPRING_DATASOURCE_URL=jdbc:{{config.datasource.type}}://db:{{#if (eq config.datasource.type "mysql")}}3306{{else}}5432{{/if}}/appdb
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=password
    depends_on:
      - db
    restart: unless-stopped

  db:
    {{#if (eq config.datasource.type "mysql")}}
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: appdb
    ports:
      - '3306:3306'
    volumes:
      - db_data:/var/lib/mysql
    {{else}}
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: appdb
    ports:
      - '5432:5432'
    volumes:
      - db_data:/var/lib/postgresql/data
    {{/if}}

volumes:
  db_data:
